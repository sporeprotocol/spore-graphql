{
  "name": "spore-graphql-repo",
  "version": "0.0.1",
  "description": "A GraphQL layer for Spore Protocol",
  "author": "Yuexun Jiang <yuexunjiang@gmail.com>",
  "license": "ISC",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run build --filter=spore-graphql",
    "prepublishOnly": "pnpm run build --filter=spore-graphql",
    "publish": "pnpm publish --access=public --filter=spore-graphql"
  }
}
